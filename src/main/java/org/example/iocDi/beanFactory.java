package org.example.iocDi;

/*
1) jab before runtime (application start hote hi) naya bean object banana ho
@Component, @Service @Repository @Controller @Bean(inside Confirguration)
2) har baar naya object - @Scope("prototype") - creates new bean object every instance
if no scope - creates Singleton - objects are reused

POJO - plain old java object

ApplicationContext - sub interface of Bean factory, provides the same features as bean factory
but provides more additional features

BeanFactory is a basic container, whereas ApplicationContext is an advanced container
ApplicationContext is an advanced container with enterprise level features
 */

public class beanFactory {

}
